{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user who has logged into the portfolio website.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The unique Firebase Auth ID of the user."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the user."
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user."
        },
        "photoURL": {
          "type": "string",
          "format": "url",
          "description": "The profile photo URL of the user."
        },
        "lastLogin": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp of the user's last login."
        }
      },
      "required": ["uid", "email", "lastLogin"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "A collection of user profiles."
    }
  }
}
